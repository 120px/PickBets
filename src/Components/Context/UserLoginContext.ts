import { createContext } from "react";

export const UserLoginContext = createContext<any>(null)

// const userLoginContext = createContext({
//     currentUser: null
// })

// export default userLoginContext 